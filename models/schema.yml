version: 2

sources:
  - name: raw          # alias du dataset gz_raw_data
    database: gz_raw_data
    schema: gz_raw_data   # nom réel du dataset dans BigQuery
    tables:
      - name: sales       # alias dbt
        identifier: raw_gz_sales   # nom réel de la table
      - name: product
        identifier: raw_gz_product
      - name: ship
        identifier: raw_gz_ship

